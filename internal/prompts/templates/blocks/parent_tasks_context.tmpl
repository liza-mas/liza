{{- define "parent-tasks-context" -}}
{{- if gt (len .ParentTaskContexts) 0}}

=== PARENT TASKS ({{len .ParentTaskContexts}}) ===
This task consolidates {{len .ParentTaskContexts}} approved upstream task(s):
{{template "artifact-ref-fallback" .}}
{{- range .ParentTaskContexts}}

--- {{.ID}} ---
DESCRIPTION: {{.Description}}
DONE WHEN: {{.DoneWhen}}
{{- if ne .SpecRef ""}}
SPEC: {{.SpecRef}}
{{- end}}
{{- if ne .EpicRef ""}}
EPIC: {{.EpicRef}}
{{- end}}
{{- if ne .PlanRef ""}}
PLAN: {{.PlanRef}}
{{- end}}
{{- end}}

For strict carriers, use the rendered local carrier content and declared direct
references. Do not recursively read ancestor artifacts or sibling sections.
Follow the legacy fallback above only for marker-free refs that are not already
presented in resolved context.
{{- end}}
{{- end -}}
