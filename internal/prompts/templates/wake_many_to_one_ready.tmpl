{{- define "wake_many_to_one_ready" -}}
All tasks in a many-to-one cohort have been merged. The cohort is ready for transition.

Pipeline transitions will execute automatically after checkpoint and human resume.
No manual task creation is needed.

You have FULL autonomy to run CLI commands immediately; do not wait for permission.

--- MANY-TO-ONE COHORT READY. NOW EXECUTE. ---

1. Run: {{binaryName}} update-sprint-metrics --json

2. Create checkpoint for human review: {{binaryName}} sprint-checkpoint --json
   The checkpoint gates downstream task creation and generates a sprint summary.

SUCCESS CRITERIA:
- Sprint checkpointed for human review.

{{template "delivery_report" .}}

❌ INCORRECT (DO NOT DO THIS):
1. Create tasks manually — the supervisor handles pipeline transitions after resume.
2. Exit without checkpointing.
{{- end -}}
