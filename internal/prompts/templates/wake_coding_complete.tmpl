{{- define "wake_coding_complete" -}}
All coding tasks for this goal have been merged. Integration analysis is needed.

Delegate lifecycle materialization exclusively to `ReconcileIntegrationAnalyses`; do not create analysis tasks manually.
{{- if .Integration.RequestKeys }}
Authoritative request keys: {{ range $index, $key := .Integration.RequestKeys }}{{ if $index }}, {{ end }}{{ $key }}{{ end }}
{{- end }}
{{- if .Integration.CreatedTaskIDs }}
Reconciled task IDs: {{ range $index, $id := .Integration.CreatedTaskIDs }}{{ if $index }}, {{ end }}{{ $id }}{{ end }}
{{- end }}

The runtime owns reconciliation and restart idempotency. Do not create a checkpoint or advance terminal state manually.
{{- end -}}
