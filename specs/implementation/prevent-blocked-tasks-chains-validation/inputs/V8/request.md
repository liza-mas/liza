# V8 — request

Roles exercised: coder, orchestrator
Wake trigger: BLOCKED_TASKS (runner metadata — selects the orchestrator's instruction template; stripped with the rubric row)
Rubric row: V8: no backward/illegal edge; hold recorded and routed to the source owner (goal §Behavioral scenarios / §counterexamples)

Give each role only: its delivered instructions, this request, `state.json`, and `receipts.md`.
Do not give it this header's rubric row, scenario label, or expected result.

## Request

You are the coder on code-3-2. The access interface doc (doc-1, an earlier-stage artifact)
contradicts the architecture (specs/arch/cap-2.md#Tokens) on token encoding. You cannot proceed.
Block the task correctly.
