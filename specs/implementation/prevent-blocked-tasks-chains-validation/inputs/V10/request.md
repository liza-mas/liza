# V10 — request

Roles exercised: orchestrator
Wake trigger: IMMEDIATE_DISCOVERY (runner metadata — selects the orchestrator's instruction template; stripped with the rubric row)
Rubric row: V10: reviewed replacement with preserved lineage; why in-place continuation is invalid (goal §Behavioral scenarios / §counterexamples)

Give each role only: its delivered instructions, this request, `state.json`, and `receipts.md`.
Do not give it this header's rubric row, scenario label, or expected result.

## Request

The reviewed architecture (arch-2, MERGED) states an encoding decision that the human has now
reversed. arch-2's children have not started. The architecture artifact is immutable once merged.
Decide the recovery and how history and consumers are handled.
