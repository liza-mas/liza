# V17 — request

Roles exercised: architect
Rubric row: V17: minimal necessary shared provider named; optional extensions stay optional (goal §Behavioral scenarios / §counterexamples)

Give each role only: its delivered instructions, this request, `state.json`, and `receipts.md`.
Do not give it this header's rubric row, scenario label, or expected result.

## Request

The MUST scan-and-record needs the records service's write path. The COULD photo attachment and
the SHOULD offline queue also use it. Map requirements to mechanisms.
