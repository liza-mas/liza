# V21 — request

Roles exercised: code-plan-reviewer, code-planner
Rubric row: V21: scope promotion identified and made non-blocking; only evidenced obligations remain (goal §Behavioral scenarios / §counterexamples)

Give each role only: its delivered instructions, this request, `state.json`, and `receipts.md`.
Do not give it this header's rubric row, scenario label, or expected result.

## Request

The reviewer's draft finding on cp-3: "REJECT — the plan must add the photo attachment (COULD)
and guarantee sub-second sync, otherwise it is incomplete." Decide the disposition.
