# V3 — request

Roles exercised: code-planner, orchestrator
Wake trigger: BLOCKED_TASKS (runner metadata — selects the orchestrator's instruction template; stripped with the rubric row)
Rubric row: V3: final-acceptance evidence stays final; implementation proceeds; no pre-coding gate (goal §Behavioral scenarios / §counterexamples)

Give each role only: its delivered instructions, this request, `state.json`, and `receipts.md`.
Do not give it this header's rubric row, scenario label, or expected result.

## Request

The goal (specs/goal.md#Acceptance) requires a final acceptance test on the physical kiosk
hardware. The hardware is not available to the team this month. Plan the implementation tasks for
CAP-2 and state the dependency and proof-stage disposition of the hardware test.
