# Architecture Plan — Pre-Commit Bootstrap: Prompts Narrowing + Architect-Role Update + Greenfield Reproduction

**Task:** `architecture-3`
**Goal spec:** `specs/goals/20260417-precommit-bootstrap.md`
**Scope (this task):** Planning only. Prompt template wording (base + architect role) and greenfield reproduction procedure. **Out of scope:** schema (`architecture-1`), detection/context package (`architecture-2`), ADR-0036 amendment (`architecture-4`).

This document plans three independently-implementable code-planning scopes covering goal-spec acceptance criteria #1 (greenfield reproduction), #2 (architect prompt requires install-authorization clause when emitting `kind: bootstrap-precommit`), and #5 (`base_prompt.tmpl:36` replacement).

---

## 1. Context & Premises

### 1.1 Sibling-task surface this plan binds to

| Sibling | Status | Surface this plan consumes |
|---|---|---|
| `architecture-1` | MERGED | `OutputEntry.Kind` and `Task.Kind` fields exist; `proceed.go` performs authoritative kind-based dedup. Implementation merges before any code-planning task here is implemented. |
| `architecture-2` | REVIEWING_ARCHITECTURE (iteration 2) | Plans to add three architect-only `RoleContextData` fields: `PreCommitConfigExists bool`, `PreCommitBootstrapInFlight bool`, `PreCommitKind string`. The third field exists explicitly so prompt templates can reference the constant without hardcoding the literal. |
| `architecture-4` | DRAFT_ARCHITECTURE | ADR-0036 amendment for the `kind` extension. Documentation only; does not change the surface this plan touches. |

**Coupling to `architecture-2`:** scope §2 below references `{{.PreCommitKind}}`, `{{.PreCommitConfigExists}}`, `{{.PreCommitBootstrapInFlight}}` as templated values. These names are the contract `architecture-2` is designing toward in iteration 2 (post-rejection fix is unrelated to field names — see `architecture-2` rejection: blockers were `cat-file -e` exit-code semantics and `return nil` vs `continue` in supervisor). If `architecture-2` review changes any of these three field names, the scope §2 code-planning task absorbs the rename mechanically; the design here is name-stable.

**Coupling to `architecture-1`:** scope §2 emits a `kind` field example in the output-entry JSON schema shown in `architect_tools.tmpl`. The field exists post-merge. No template change in scope §2 is meaningful until `architecture-1`'s `kind` field has reached the architect's persisted context — but `architecture-1` is already MERGED, so the dependency is satisfied at plan time.

### 1.2 Why the install-authorization clause goes in the *prompt*, not the spec only

Goal-spec line 41-44 defines the clause text. The spec is read by humans drafting the design; the architect agent reads its own prompt. Without surfacing the clause in the architect prompt, the architect would have to re-derive the wording from the spec on every emission — assumption-budget waste and a drift surface (paraphrase risk). The spec wording is canonical; the prompt **quotes it verbatim** with a "MUST include verbatim or substantively equivalent" constraint, and the spec stays the single source of truth via cross-reference.

### 1.3 Why the bootstrap `done_when` phrasing belongs in the architect prompt

The bootstrap task's `done_when` is two invariants:

- pre-commit runs cleanly: `<dep-manager-runner> pre-commit run --all-files` exits 0 against the bootstrap commit;
- pre-commit is recorded as a dev dependency in the project's dep-manager manifest.

The first invariant is stack-specific (`uv run pre-commit run …`, `npm run pre-commit …`, `python -m pre_commit run …`). The second is stack-specific (`pyproject.toml`, `package.json`, `Cargo.toml`). The architect picks the stack and therefore phrases the `done_when`. The prompt template gives the **template** ("two invariants the architect must specialize"), not the literal stack-specific text — this preserves G1.1 (stack-agnostic prompts) while ensuring the architect captures both invariants.

### 1.4 Why a separate `bootstrap_precommit_guidance.tmpl` block is **rejected**

Considered: extract bootstrap-precommit-specific guidance into a new template block, included via pipeline.yaml `context-sections`. **Rejected** because:

- Adds a new file + a pipeline.yaml edit for two paragraphs of text (G2.2: contract conciseness — growth must not exceed semantic content added).
- The architect role has exactly one consumer for this guidance today; YAGNI.
- Future generalization (`.editorconfig`, lint config) is `Out of Scope` of the goal spec; designing a shared-block surface now is speculation.

If a second bootstrap-style primitive arrives, the extraction is a one-task refactor with clear precedent. Until then, the guidance lives inside the architect branch of `implementation_phase.tmpl`.

### 1.5 Greenfield reproduction is a goal-spec acceptance criterion, not a code-planning criterion

Goal-spec acceptance criterion #1 is a **hard gate on the goal**: implementation may not land without it. It is not a per-code-planning-task gate; it is a one-shot artifact captured once for the whole goal. This plan therefore emits the procedure as a single code-planning scope (§3) producing two artifacts: (a) the procedure document and (b) a captured-observations appendix appended to the goal spec.

The "captured-observations appendix" replaces the hypothesized list in goal spec §Evidence (lines 13-17). Per goal-spec §Evidence: "If reproduction shows the actual failure mode is different from any of the above, the ratified design is revisited before implementation proceeds." Therefore the procedure scope must include a step where the observations are reviewed against the design and either confirm-or-divert is recorded.

**Sequencing within the goal:** scopes §1 and §2 of this plan can be implemented in parallel with §3, but the goal-level acceptance criterion #1 means **none of the code from any goal-wide architecture task may MERGE into integration before §3's reproduction-and-observations artifact is committed.** That cross-task ordering is the orchestrator's responsibility to enforce (via dependency wiring or an explicit gate). This plan documents the requirement; it does not encode the cross-architect dependency in `depends_on` (which is sibling-only).

---

## 2. Component Boundaries & Decomposition

Three code-planning scopes. Each becomes one downstream code-planning task → one or more coding tasks via the standard pipeline.

| # | Scope | Files touched | Independent of siblings? |
|---|---|---|---|
| 0 | Base-prompt narrowing (`base_prompt.tmpl:36`) | `internal/prompts/templates/base_prompt.tmpl`, `internal/prompts/builder_test.go` (assertion only) | Yes — independent |
| 1 | Architect-role prompt delta (install-authorization clause + bootstrap `done_when` phrasing + omission rule referencing context flags) | `internal/prompts/templates/blocks/architect_tools.tmpl`, `internal/prompts/templates/blocks/implementation_phase.tmpl` (architect branch only) | Yes — different files from §0; consumes `architecture-1` and `architecture-2` surface |
| 2 | Greenfield reproduction **scaffolding** (procedure doc + capture script + observation-table placeholder) | `specs/goals/20260417-precommit-bootstrap.md` (new sections), `scripts/repro/precommit-bootstrap-greenfield.sh` (new) | Yes — touches no template files |
| 3 | Greenfield reproduction **execution** (run procedure, commit artifacts, populate observation table, write confirm-or-divert decision) | `specs/goals/20260417-precommit-bootstrap.md` (table cells in section added by §2), `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/` (new artifact directory, contents committed) | Depends on §2 (`depends_on: ["2"]`) — needs the script and the placeholder section |

§2 and §3 share `specs/goals/20260417-precommit-bootstrap.md`. The dependency `depends_on: ["2"]` enforces ordering and is required (per pre-submit self-check 9f shared-file audit). §3 mutates only the table cells and the "Confirm-or-Divert Decision" / "Newly observed modes" subsections — sections §2 creates as placeholders. §3 does NOT modify the procedure-step text §2 writes.

§0 and §1 have no shared file with each other or with §2/§3. No depends_on between {§0, §1} is required.

**Why split §2 and §3 rather than collapse:** the reviewer's blocker — no output owns the actual reproduction run — is fixed by making §3 the explicit owner with a falsifiable done_when keyed on populated cells, committed artifacts, and a non-placeholder decision paragraph. The split also keeps each task atomic (one intent: §2 builds the harness, §3 runs it) and allows §2 to be reviewed/merged in parallel with §0/§1 while §3 sequences after §2 lands.

### 2.1 Cross-scope invariant: spec wording fidelity

Scopes §0 and §1 quote text from `specs/goals/20260417-precommit-bootstrap.md`. The downstream code-planning self-check (per `implementation_phase.tmpl` code-planner branch step 6e) requires character-identical extraction. The exact text appears verbatim in §3, §4 below — code-planners and coders extract from this arch doc, not from memory.

---

## 3. Scope §0 — Base-Prompt Narrowing

### 3.1 Intent

Replace `internal/prompts/templates/base_prompt.tmpl:36` with the narrowed wording from goal-spec §Preconditions / Prompt wording to narrow.

### 3.2 Current line (verbatim)

`internal/prompts/templates/base_prompt.tmpl:36`:

```
- NEVER attempt to install, bootstrap, or fix system-level tooling.
```

### 3.3 Replacement text (verbatim from goal-spec lines 25-27)

The line is replaced with the following multi-line bullet, preserving the leading `- ` and the surrounding `BASH CONSTRAINTS` block context (lines 30-38 of `base_prompt.tmpl`):

```
- NEVER install OS packages, language runtimes, IDE tooling, or global developer tools by default. Project-level config files (`.pre-commit-config.yaml`, `.editorconfig`, lint configs) are project work. Tools required to run those configs may be provisioned only when the task's explicit scope authorizes a project-scoped setup path; otherwise mark BLOCKED rather than mutating the host environment.
```

**Justified minor edit:** none. The replacement is goal-spec lines 25-27 verbatim, including the inline-code formatting of the three example filenames.

### 3.4 Insertion mechanics

- The line is at `base_prompt.tmpl:36` between the `NEVER use ! inside double-quoted strings` bullet (line 34-35) and the `NEVER use "git add -A"` bullet (line 37). No reordering.
- The replacement is one bullet, one line in the source (line wrap is fine — Markdown rendering is not part of the contract). Keep the leading hyphen and single space; do not add nested sub-bullets.
- No template-data variables are introduced. The wording is static for all roles.

### 3.5 done_when (for the downstream code-planning task)

`internal/prompts/templates/base_prompt.tmpl` line 36 (1-based, post-edit) contains the replacement text from §3.3 of the architecture document `specs/arch-plan/20260417-145405-architecture-3.md`, character-identical including backticks around the three filenames; `internal/prompts/builder_test.go:548` assertion string is updated to match a stable substring of the new wording (e.g. `"NEVER install OS packages, language runtimes, IDE tooling"` and `"Project-level config files"` and `"mark BLOCKED rather than mutating the host environment"` — three substring assertions replacing the single old-wording assertion); `go test ./internal/prompts/...` passes.

### 3.6 Risks & non-risks

- **Non-risk: cascading prompt drift.** The replacement is purely textual; `RoleContextData` is unchanged.
- **Non-risk: rendering breakage.** No new template actions (`{{ … }}`); plain text.
- **Risk: prompt-snapshot test churn.** If `internal/agent/prompt_test.go` snapshots the rendered base prompt, the test must be updated to match. The downstream code-planning task must enumerate every snapshot/golden file that contains the old line and update each.

---

## 4. Scope §1 — Architect-Role Prompt Delta

### 4.1 Intent

Update the architect-role prompt to:

- (a) document `kind` as an optional `OutputEntry` field with `bootstrap-precommit` as the initial valid value;
- (b) surface the omission rule keyed off `{{.PreCommitConfigExists}}` and `{{.PreCommitBootstrapInFlight}}`;
- (c) require the install-authorization clause (verbatim from goal-spec §Preconditions lines 41-44) in the `desc` of any emitted `kind: bootstrap-precommit` entry;
- (d) require the bootstrap `done_when` to capture two invariants (clean pre-commit run + dep-manager dev-dependency record), specialized for the chosen stack.

### 4.2 Files touched

| File | Edit |
|---|---|
| `internal/prompts/templates/blocks/architect_tools.tmpl` | Extend the JSON schema example (lines 13-15) to include `"kind"` as an optional field; add a one-sentence pointer to the bootstrap-precommit guidance in `implementation_phase.tmpl`. |
| `internal/prompts/templates/blocks/implementation_phase.tmpl` (architect branch, lines 133-169) | Insert a new "BOOTSTRAP-PRECOMMIT REQUIREMENTS" sub-section between step 6 (output emission) and step 7 (worktree-artifact updates). |

No new template files. No pipeline.yaml change. Both files are already included in the architect role's `context-sections` (verified in `internal/agent/prompt_test.go:1962-1965`).

### 4.3 `architect_tools.tmpl` delta — exact wording

Change the JSON schema example (lines 13-15 of the current file) from:

```
  JSON file contains: [
    {"desc": "...", "done_when": "...", "scope": "...", "spec_ref": "...", "arch_ref": "specs/arch-plan/<doc>.md", "depends_on": ["0", "2"]}
  ]
```

To:

```
  JSON file contains: [
    {"desc": "...", "done_when": "...", "scope": "...", "spec_ref": "...", "arch_ref": "specs/arch-plan/<doc>.md", "depends_on": ["0", "2"], "kind": "<optional typed marker — see BOOTSTRAP-PRECOMMIT REQUIREMENTS in IMPLEMENTATION PHASE>"}
  ]
```

(The "see …" reference is the bridge to the new sub-section in §4.4 below. Keeping the schema line tight and pointing to the longer guidance keeps `architect_tools.tmpl` from carrying paragraph-length text — G2.2.)

### 4.4 `implementation_phase.tmpl` delta — exact wording and insertion point

**Insertion point:** between current step 6 (`For EACH code-planning scope … set-task-output …`, lines 154-159) and current step 7 (`Update worktree artifacts required by DONE WHEN`, line 160) of the `architect` branch.

**New sub-section text (inserted as an unnumbered block; subsequent steps renumber if the implementer prefers numbered steps — both shapes pass the prompt's existing convention):**

```
BOOTSTRAP-PRECOMMIT REQUIREMENTS:
{{- if eq .Role "architect"}}
A bootstrap-precommit entry is the typed marker the pipeline uses to dedup pre-commit-config bootstrap across replans, parallel architect invocations, and cross-goal races. Emit at most one such entry per architect invocation, and only when neither omission condition below holds.

OMISSION RULES (skip the bootstrap entry when either is true):
- PreCommitConfigExists = {{.PreCommitConfigExists}}: integration branch HEAD already has .pre-commit-config.yaml.
- PreCommitBootstrapInFlight = {{.PreCommitBootstrapInFlight}}: a non-terminal task repo-wide already carries kind: {{.PreCommitKind}}.
If either flag is true, do NOT emit a bootstrap entry. Other output entries declare depends_on relative to the surviving entry indices (no synthetic depends_on: ["0"] is added when bootstrap is omitted).

EMIT RULES (when both flags are false and the project lacks a pre-commit config):
- Set "kind": "{{.PreCommitKind}}" on the bootstrap entry. Place it as output[0]; all other architect output entries declare depends_on: ["0"]. Phase-gate inheritance propagates the dependency to coding tasks.
- desc MUST include the install-authorization clause verbatim or substantively equivalent (single source of truth: specs/goals/20260417-precommit-bootstrap.md#preconditions, lines 41-44):
    "This task may provision pre-commit only via a project-scoped mechanism already available in the repo or explicitly defined by the task/config. Do not install OS packages or unrelated global tooling. If no authorized project-scoped path exists, mark BLOCKED."
- done_when MUST assert two invariants, specialized for the chosen stack:
    (a) <dep-manager-runner> pre-commit run --all-files exits 0 against the bootstrap commit (e.g. `uv run pre-commit run --all-files`, `npm run pre-commit -- --all-files`, `python -m pre_commit run --all-files`);
    (b) the project's dep-manager manifest (e.g. pyproject.toml, package.json) records pre-commit as a dev dependency.
- Hook-set selection: the architect picks stack-appropriate hook categories and concrete hooks in free-form prose inside desc. Do NOT defer to a default hook list — there is no default. Do NOT include version pins (deferred to a later hook-selection task per goal-spec Out of Scope).
- Stack-specific examples live in the goal spec, never in this prompt (G1.1: prompt is stack-agnostic).
{{- end}}
```

**Conditional gating rationale:** the `{{- if eq .Role "architect"}}` guard inside the architect branch of `implementation-phase` is technically redundant (the surrounding `{{- else if eq .Role "architect"}}` branch already gates) but is included to make the sub-section's role-specificity explicit and to allow a future code-planner branch to re-use a similar block by lifting only the inner content.

### 4.5 Why the install-authorization clause "fires only for kind: bootstrap-precommit"

The clause is **always present in the prompt**, but the prompt itself instructs the architect: "Set kind: {{.PreCommitKind}} on the bootstrap entry … desc MUST include the install-authorization clause." The conditional fires at *emission* time, in the architect's reasoning, not at *render* time. This satisfies acceptance criterion #2 — the requirement is **wording-conditional** on `kind`, not template-conditional on omission flags.

**Alternative considered and rejected:** render-time conditional that includes the clause text only when the architect is "about to emit" a bootstrap entry. Rejected because the architect's emission decision happens *after* prompt render — the template cannot know in advance. A render-time conditional would either always include the clause (current design) or always omit it (defeats the purpose).

### 4.6 done_when (for the downstream code-planning task)

`internal/prompts/templates/blocks/architect_tools.tmpl` JSON schema line includes `"kind"` as the seventh field with the verbatim hint string from §4.3 of `specs/arch-plan/20260417-145405-architecture-3.md`; `internal/prompts/templates/blocks/implementation_phase.tmpl` architect branch contains the BOOTSTRAP-PRECOMMIT REQUIREMENTS sub-section with text character-identical to §4.4 (including the verbatim install-authorization clause from goal-spec lines 41-44); template renders without error for the architect role with `RoleContextData{Role: "architect", PreCommitKind: "bootstrap-precommit", PreCommitConfigExists: false, PreCommitBootstrapInFlight: false}`; rendered output contains the literal substring "Set \"kind\": \"bootstrap-precommit\""; rendered output for a non-architect role (e.g. coder) does NOT contain "BOOTSTRAP-PRECOMMIT REQUIREMENTS"; `go test ./internal/prompts/... ./internal/agent/...` passes; existing snapshot/golden tests of architect prompts are updated to reflect the new sub-section.

### 4.7 Risks

- **Risk: template-data dependency on `architecture-2` not yet merged.** The downstream code-planning task and its coding task must wait for `architecture-2`'s `RoleContextData` extension to merge. Mitigation: phase-gate inheritance gates downstream coding tasks on architecture approval; orchestrator wires `architecture-2`-output coding tasks to merge before `architecture-3`-output coding tasks via cross-architect ordering. This plan documents the requirement; the orchestrator owns enforcement.
- **Risk: field rename in `architecture-2` review.** If iteration 2 of `architecture-2` renames `PreCommitConfigExists`, `PreCommitBootstrapInFlight`, or `PreCommitKind`, the template references in §4.4 require a search-and-replace. Mitigation: the references are mechanical and uniquely searchable; a reviewer-or-coder pass catches them.
- **Risk: G2.2 violation (prompt growth).** The new sub-section is roughly 18 lines of guidance. Comparison: the spec text it operationalizes spans goal-spec lines 41-44 (install-authorization), 96-110 (Q3 architect prompt requirements), 138-159 (worked example). The prompt distillation is ~12% of the spec material — comfortably below "growth exceeds semantic content added."

---

## 5. Scopes §2 + §3 — Greenfield Reproduction (Scaffolding + Execution)

### 5.0 Why two scopes

Acceptance criterion #1 has two distinct deliverables: (a) a reproducible procedure and capture infrastructure, (b) the actual run that produces the evidence the criterion demands. Collapsing them into one task would make the task non-atomic (planning + live evidence-gathering in one unit) and would make `done_when` ambiguous about whether the harness or the evidence is the deliverable. Splitting:

- **§2 (output[2])** — scaffolding. Atomic, mostly-mechanical: writes the procedure text, the script, and the placeholder table. Reviewable from the diff alone.
- **§3 (output[3])** — execution. Depends on §2. Runs the procedure (live agent runs against a synthetic greenfield project), captures artifacts, populates the observation table with evidence pointers, and commits the confirm-or-divert decision. Reviewable by inspecting the populated table and the committed artifact directory.

§3 owns acceptance criterion #1 — the "no output owns the hard gate" failure mode is closed.

### 5.1 Intent

**Together, scopes §2 and §3 deliver a procedure that:**

- Sets up a greenfield project (no `.pre-commit-config.yaml`, no `.liza/`, no pre-commit binary on PATH inside the project sandbox) and runs the goal pipeline through architect → code-planner → coder.
- Captures observed failure modes against the four hypothesized modes in goal-spec lines 14-17.
- Records observations in a designated location that supersedes the hypothesized list.
- Provides a confirm-or-divert decision artifact that gates implementation per acceptance criterion #1.

### 5.2 Procedure document location and shape

**Location:** `specs/goals/20260417-precommit-bootstrap.md` gains a new section **"Greenfield Reproduction Procedure"** appended after `## Worked Example (Python + uv)` and before `## Out of Scope`. Rationale: keeping the procedure with the goal spec preserves single-source traceability; downstream readers find it where they look for goal scope.

**Section structure (verbatim subsection headings the downstream task will write):**

```
## Greenfield Reproduction Procedure

### Setup
### Execution
### Capture
### Observation Recording
### Confirm-or-Divert Decision
```

### 5.3 Procedure steps (verbatim text the downstream task writes into §Setup, §Execution, §Capture)

**§Setup:**

1. Create a scratch directory outside any liza-managed worktree: `REPRO_ROOT=$(mktemp -d -t liza-precommit-repro-XXXXXX)`.
2. Initialize a minimal greenfield project: `git init "$REPRO_ROOT/proj" && cd "$REPRO_ROOT/proj"`. Add a single-line `README.md` and a vision document at `specs/vision/greenfield.md` describing one trivial feature ("add `hello` script printing 'hi'"). Commit both as the initial commit.
3. Run `liza init` against `"$REPRO_ROOT/proj"` with the standard pipeline. Confirm `state.yaml` is created, no `.pre-commit-config.yaml` exists, and `command -v pre-commit` outside any project-scoped venv either returns nothing or returns a path the procedure agrees to ignore for the purposes of this run.
4. Configure the supervisor for verbose logging: `LIZA_LOG_LEVEL=debug` and `LIZA_TEE_AGENT_OUTPUT=1` (or equivalent — the script in §5.4 captures the active tee/log paths).

**§Execution:**

5. Start the supervisor for one full goal cycle (orchestrator emits planning → architect → architecture-reviewer → code-planner → code-plan-reviewer → coder → code-reviewer → integration). Allow it to run until either (a) the integration task reaches a terminal state, or (b) any task transitions to BLOCKED with a reason mentioning pre-commit, missing config, or install authorization.
6. Run a second supervisor cycle in parallel against a sibling scratch repo (`REPRO_ROOT/proj-parallel`) seeded identically, to exercise the parallel-coders failure mode. The second cycle uses a separate `state.yaml` and worktree set so the two cycles cannot accidentally cooperate.

**§Capture:**

For each cycle, capture the following artifacts under `"$REPRO_ROOT/observations/<cycle-id>/"`:

7. Full `state.yaml` snapshots at three points: post-init, post-architect-merge, post-coder-attempt (or terminal state).
8. The complete contents of `.liza/agent-outputs/` (every checkpoint, every set-task-output payload, every verdict).
9. The supervisor log (`.liza/supervisor.log` or stdout capture) and any RTK tee files referenced in `~/.local/share/rtk/tee/` whose timestamps fall within the cycle window.
10. `git log --all --oneline --graph` for every worktree under `.worktrees/`.
11. The full prompt rendered for each agent invocation (already captured in agent-outputs when `LIZA_TEE_AGENT_OUTPUT=1`; verify each role appears at least once).
12. A diff `git diff <initial-commit>..HEAD -- .pre-commit-config.yaml` for every worktree, plus a project-root listing showing whether `.pre-commit-config.yaml` exists at the integration branch HEAD.

### 5.4 Capture script

**Location:** `scripts/repro/precommit-bootstrap-greenfield.sh` (new). Bash script. Single argument: `$1 = REPRO_ROOT`.

**Responsibility:** automate steps 1-4 (Setup) and steps 7-12 (Capture). Steps 5-6 (Execution) require live supervisor runs and are documented as manual steps in §Execution; the script captures whatever state exists when the operator runs the capture phase.

**Script outline (the downstream code-planning task writes the implementation):**

- Validate `$REPRO_ROOT` exists and is empty.
- Create `proj/` and `proj-parallel/`, seed identical initial commits.
- Print a banner instructing the operator to run two supervisor cycles in parallel; wait for an explicit `OBSERVATIONS_READY=1` env-var or interactive `[Enter]` before proceeding to capture.
- For each project, snapshot `state.yaml` (current state only — earlier snapshots must be taken by the supervisor's audit hooks if they exist, otherwise the operator captures them manually before triggering the script's capture phase), copy `.liza/agent-outputs/`, copy supervisor log, run `git log` for each worktree, write all artifacts under `$REPRO_ROOT/observations/<proj>/`.
- Emit a summary `observations.md` that enumerates artifact paths and asks the operator to fill in the four "observed?" rows in the §Observation Recording table.

**Hard constraints:**

- Script must not install anything host-level. It uses only `git`, `mktemp`, `cp`, `find`, plus standard POSIX utilities.
- Script is read-only against the operator's actual liza state; it operates entirely under `$REPRO_ROOT`.
- Script exit code 0 means "capture phase completed and artifacts written"; non-zero means a capture step failed (operator inspects which).

### 5.5 Observation recording

**Location:** the goal spec gains a new section **"Observed Failure Modes — Reproduction Run YYYY-MM-DD"** appended after the new "Greenfield Reproduction Procedure" section. The section structure:

```markdown
## Observed Failure Modes — Reproduction Run YYYY-MM-DD

Reproduction artifacts: `<path under repo, e.g. specs/goals/precommit-bootstrap-repro-artifacts/>` (committed as part of this run).

| # | Hypothesized mode (goal spec §Evidence, lines 14-17) | Observed? (yes/no/partial) | Evidence pointer | Notes |
|---|---|---|---|---|
| 1 | Coders inventing `.pre-commit-config.yaml` ad-hoc inside an unrelated coding task | … | `<artifact path>:<line>` | … |
| 2 | Parallel coders producing conflicting configs | … | … | … |
| 3 | Coders silently skipping pre-commit when it errors with "no config" | … | … | … |
| 4 | DoD step "Pre-commit passes on touched files" (CORE.md Rule 3) becoming vacuous | … | … | … |

### Newly observed modes (not in the hypothesized list)
<list of any additional failure modes discovered during reproduction; if none, write "None observed.">

### Confirm-or-Divert Decision
<one paragraph: either "Hypothesized modes confirmed; design ratified as written" or "Observed modes diverge from hypothesis: <summary>; design revisited at <link to revised section / new spec>.">
```

The "Newly observed modes" subsection is mandatory — even an empty "None observed" line is required so the absence is explicit.

### 5.6 Reproduction-artifacts commit policy

The captured artifacts (state snapshots, agent-outputs copies, logs) live under `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/`. They are committed to the repo as part of the reproduction-run code-planning task's deliverable. Rationale: future goal revisits and reviewers inspect the same evidence the original decision rested on.

**Sanitization:** before commit, the operator scrubs absolute filesystem paths (`/home/<user>/...`) by replacing the user-home prefix with `~`. No secrets are expected in the captured material — the procedure runs against a synthetic vision doc and a fresh git identity — but the downstream coding task adds a pre-commit check (or a manual review checklist) confirming no `.env`, `*.key`, or `credentials*` files appear under the artifacts directory.

### 5.7 Cross-architect ordering (orchestrator-enforced)

Per §1.5, goal-level acceptance criterion #1 means **none of the code from any goal-wide architecture task may MERGE into integration before §3 (output[3]) reaches MERGED.** Specifically: coding tasks descended from `architecture-1` (kind field + dedup), `architecture-2` (detection package + context enrichment), `architecture-3 §0/§1` (this plan's first two prompt scopes), and `architecture-4` (ADR amendment) all remain in non-MERGED status until output[3] of this plan lands.

Enforcement options (orchestrator picks one when wiring downstream tasks):

- (a) **Explicit dependency wiring (preferred).** Orchestrator emits `depends_on` referencing the output[3] coding task ID on every other goal-wide coding task descended from `architecture-1`, `architecture-2`, `architecture-3` (output[0], output[1]), and `architecture-4`. Phase-gate inheritance handles propagation. Falsifiable: the dependency graph encodes the gate.
- (b) **Integration-reviewer gate (fallback).** Integration reviewer checks the goal-spec "Observed Failure Modes" table for **populated cells** (no `…` placeholders in the four hypothesized-mode rows) AND a non-placeholder "Confirm-or-Divert Decision" paragraph (matching one of the two sentence patterns in §5.5) before approving any other goal-wide integration. **Crucially, presence of the section header alone is insufficient — the §2 scaffolding creates the section as a placeholder.** The gate is satisfied only when the cells contain evidence-backed yes/no/partial values plus artifact pointers.

This plan documents the requirement and both enforcement options; choosing (a) vs (b) is an orchestrator-policy decision outside the scope of this architecture document. Either way, output[3]'s `done_when` (§5.9) is the falsifiable contract on what "populated" means.

### 5.8 done_when — Scope §2 / output[2] (scaffolding)

`specs/goals/20260417-precommit-bootstrap.md` contains the new "Greenfield Reproduction Procedure" section with subsection headings exactly as listed in §5.2 of `specs/arch-plan/20260417-145405-architecture-3.md` and step text character-identical to §5.3; `scripts/repro/precommit-bootstrap-greenfield.sh` exists, is executable (`chmod +x`), implements the responsibilities listed in §5.4, and exits 0 against an empty `$REPRO_ROOT` after a dry-run integration test that mocks the operator gate (script contains no install commands or curl/wget invocations); the goal spec contains a placeholder "Observed Failure Modes — Reproduction Run YYYY-MM-DD" section with the table structure from §5.5 — every table cell in the four hypothesized-mode rows contains the literal placeholder character `…` (so output[3] can detect "populated" by absence of `…`); the "Newly observed modes" subsection contains the literal text `<list of any additional failure modes discovered during reproduction; if none, write "None observed.">`; the "Confirm-or-Divert Decision" subsection contains the literal placeholder paragraph from §5.5; a one-line cross-reference is added to goal-spec §Evidence (after line 17) reading "Observed modes recorded in §Observed Failure Modes — Reproduction Run after reproduction lands." `go test ./...` is unaffected; no liza-internal Go code changes.

### 5.9 done_when — Scope §3 / output[3] (execution)

The reproduction procedure scaffolded by output[2] has been executed against a fresh synthetic greenfield project per §Setup/§Execution; artifacts have been captured per §Capture and committed under `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/` (the date in the path matches the date in the goal-spec section header); the artifact directory contains, at minimum: `state-snapshots/` with three `state-*.yaml` files (post-init, post-architect-merge, post-coder-attempt or terminal), `agent-outputs/` (copy of `.liza/agent-outputs/` from the reproduced project's lifecycle), `supervisor.log`, and `worktree-git-logs/` (one `git log --all --oneline --graph` text file per worktree); the goal spec's "Observed Failure Modes — Reproduction Run YYYY-MM-DD" section has its YYYY-MM-DD slot filled with the actual run date, and **every cell in the four hypothesized-mode rows is populated** (no `…` placeholder remains in the "Observed?", "Evidence pointer", "Notes" columns — each row's "Observed?" is `yes`, `no`, or `partial`; each "Evidence pointer" references a relative path under `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/`; each "Notes" cell has at least one sentence); the "Newly observed modes" subsection no longer contains the angle-bracketed placeholder — it is replaced with either a bulleted list of additional modes or the literal sentence "None observed."; the "Confirm-or-Divert Decision" subsection no longer contains the angle-bracketed placeholder — it is replaced with either "Hypothesized modes confirmed; design ratified as written." OR a paragraph beginning "Observed modes diverge from hypothesis:" followed by a summary and a link to the revised section or new spec; if the decision is "divert", a follow-up issue or task ID is referenced in the paragraph and noted in TECH_DEBT.md or as a new orchestrator-emitted task. **Falsifiability check:** running `grep -c '…' specs/goals/20260417-precommit-bootstrap.md` against the modified file returns the same count as before output[2] minus the four row-cell placeholders × three columns = 12 fewer occurrences in the four rows specifically (the procedure-step text may legitimately contain `…` in unrelated places, so the check scopes to the table region by line range or section anchor). No prompt template files are modified by output[3]. No Go source is modified.

**Risks specific to §3:**

- **Risk: live LLM cost / non-determinism.** Live runs cost API budget and may require multiple iterations to produce a clean capture. Mitigation: §Capture allows up to 5 cycles; if the first cycle is inconclusive on any row, the operator iterates.
- **Risk: divert outcome blocks the goal.** If the run shows the design needs revision, every other goal-wide coding task waits. This is the **intended** behavior per goal-spec line 19 ("the ratified design is revisited before implementation proceeds"). The orchestrator handles divert by re-planning the affected scopes.
- **Risk: artifact directory grows large.** Captured logs and agent-outputs may be hundreds of KB. Acceptable — repo growth is bounded (one-off artifact per goal) and traceability value is high. If size becomes a concern in practice, future goals can move artifacts to LFS or external storage.

### 5.10 Shared risks across §2 and §3

- **Risk: live LLM runs are non-deterministic.** Two runs may produce different observed modes. Mitigation: the spec section accepts "yes/no/partial" per row and documents `<= 5` reproduction cycles in §Capture if the first cycle is inconclusive on any row. The procedure does not require a single deterministic run; it requires evidence-backed observations.
- **Risk: parallel runs collide on shared system state.** Two supervisor processes against two scratch repos under one `$REPRO_ROOT` should be filesystem-isolated, but the two processes share `~/.local/share/rtk/tee/`. Mitigation: the capture step records tee files by timestamp window per cycle; cycle-id is part of the artifact path.
- **Risk: reproduction does not exhibit any of the four hypothesized modes.** Per goal-spec line 19 ("If reproduction shows the actual failure mode is different from any of the above, the ratified design is revisited"), this is an explicit allowed outcome. The Confirm-or-Divert subsection (§5.5) handles it; output[3]'s done_when accepts both confirm and divert paragraphs.
- **Non-risk: missing pre-commit binary.** The whole point of the reproduction is to exercise greenfield where the binary is absent. The procedure does not pre-install anything; absence is part of the test.

---

## 6. Spec Compliance — Acceptance Criteria Coverage

This plan's three scopes cover the goal spec's acceptance criteria as follows. Acceptance criteria not listed here belong to sibling architecture tasks.

| Goal-spec acceptance criterion | Covered by | Status |
|---|---|---|
| #1 Greenfield run reproduced; observed failure modes captured | Scopes §2 + §3 (this plan) | §2 delivers procedure + capture script + placeholder section; §3 executes the run, commits artifacts, and populates the observation table — explicit owner of the hard gate |
| #2 Q3 design implemented; architect prompt requires install-authorization clause when emitting `kind: bootstrap-precommit` | Scope §1 (this plan) | Install-authorization clause + kind-marker emission rules + bootstrap done_when invariants in architect prompt |
| #3 `internal/precommit/` package + RoleContextData extension | `architecture-2` | Out of scope here |
| #4 `kind` field + proceed.go dedup + cross-goal scan | `architecture-1` | Out of scope here (MERGED) |
| #5 `base_prompt.tmpl:36` replaced + ADR amend/supersede | Scope §0 (this plan, prompt portion); `architecture-4` (ADR portion) | Prompt portion delivered; ADR portion is separate |
| #6 Context-builder errors surface as task-blocking | `architecture-2` (supervisor.go modification) | Out of scope here |

---

## 7. Cross-Cutting Concerns

### 7.1 Snapshot/golden test churn

Scopes §0 and §1 modify rendered prompt text. Existing prompt snapshots in `internal/agent/prompt_test.go` (and any other golden file under `internal/`) require updates. Each downstream code-planning task includes the snapshot update in its file scope and verifies the snapshot regenerator (or manual update procedure) is followed.

### 7.2 G1.1 (no Liza-specific hardcoding) compliance

- §3.3 replacement text mentions `.pre-commit-config.yaml`, `.editorconfig`, lint configs as **examples in inline code**, not as hardcoded behavior. The base prompt has no project-stack assumption.
- §4.4 architect-prompt sub-section uses `{{.PreCommitKind}}` (templated value from `architecture-2`'s `RoleContextData` extension). The literal `bootstrap-precommit` does not appear in the template — only as an example value the architect substitutes. This is anti-drift: the constant lives in `internal/precommit/`, the template references it through `RoleContextData`.
- §5 procedure operates on a synthetic greenfield project; the script does not assume any particular dep manager or language.

### 7.3 G2.2 (contract conciseness)

- §3: net change is one bullet replacing one bullet. Token count grows by ~70 words. The added wording is purely semantic (defines the boundary the previous wording left ambiguous).
- §4: net addition is ~18 lines in `implementation_phase.tmpl` and ~1 line in `architect_tools.tmpl`. The 18-line block operationalizes goal-spec lines 41-44, 96-110, 138-159. Distillation ratio (~12%) is well within "growth ≤ semantic content added".
- §5: spec additions, not template additions. The architect/coder context budget is unaffected.

### 7.4 Prompt-injection immunity

The new architect-prompt sub-section instructs the architect on what to emit. It does not interpolate task-controlled fields into agent-instruction text (everything inside `{{ … }}` is from `RoleContextData`, populated by trusted code paths in `architecture-2`'s `internal/agent/prompt.go` modification). No new injection surface.

### 7.5 Failure modes if scopes land out of order

| Order | Effect |
|---|---|
| §0 lands before §1 | Base prompt narrowed, but architect has no `kind` field guidance yet. Architect emits no `kind: bootstrap-precommit` until §1 lands; bootstrap dedup never fires. Acceptable interim state — no incorrect behavior, just the bootstrap feature is "not yet wired up" at the architect prompt layer. |
| §1 lands before §0 | Architect-prompt sub-section refers to "install-authorization" via cross-reference to the narrowed base-prompt wording, but the base-prompt wording is still the original (host-level prohibition). Architect could be confused about whether project-scoped provisioning is allowed. **Mitigation:** scope §1's done_when does not require §0 to be merged — but the downstream code-planning task for §1 should note this in its plan-level risks. |
| §2 lands before §0 / §1 | Procedure documents a feature that is not yet implemented in the prompts. §2 by itself only adds scaffolding; §3 then runs against current (unfixed) prompts to capture *baseline* failure modes. This is exactly the design intent — confirm or deny the hypothesis against the unfixed system. |
| §3 lands before §0 / §1 / §2 | Impossible — §3 declares `depends_on: ["2"]` so §2 must merge first; §3 needs the script and placeholder section. §0 / §1 ordering vs §3 is an orchestrator concern (§5.7). |
| §0 / §1 / §2 land before §3 | Acceptance criterion #1 is satisfied only when §3 is MERGED. Cross-architect orchestrator enforcement (§5.7 option a or b) holds back integration of all other goal-wide coding tasks until output[3]'s populated table is committed. |

---

## 8. Decomposition Summary (mapping to set-task-output entries)

Four entries. `depends_on: ["2"]` on output[3] (shared file with output[2] and execution must follow scaffolding). Each is a single code-planning intent.

| Index | Scope ref | desc summary | done_when summary | Files in scope | depends_on |
|---|---|---|---|---|---|
| 0 | §3 | Replace `base_prompt.tmpl:36` with the narrowed wording from goal-spec §Preconditions lines 25-27 (verbatim). | The line is replaced character-identical; `internal/prompts/builder_test.go:548` assertion updated to three substring checks against the new wording per §3.5; tests pass. | `internal/prompts/templates/base_prompt.tmpl`, `internal/prompts/builder_test.go`. | — |
| 1 | §4 | Add the `kind` field to the architect-tools JSON schema and add the BOOTSTRAP-PRECOMMIT REQUIREMENTS sub-section to the architect branch of `implementation_phase.tmpl`, including verbatim install-authorization clause and bootstrap `done_when` invariant pattern. | Both template files contain the text from §4.3 and §4.4 character-identical; rendered architect prompt contains the substring "Set \"kind\": \"bootstrap-precommit\""; non-architect roles do not render the sub-section; tests pass. | `internal/prompts/templates/blocks/architect_tools.tmpl`, `internal/prompts/templates/blocks/implementation_phase.tmpl`, plus any prompt-snapshot test affected. | — |
| 2 | §5 (scaffolding) | Add the "Greenfield Reproduction Procedure" section to the goal spec with the structure from §5.2 and step text from §5.3; add the "Observed Failure Modes — Reproduction Run YYYY-MM-DD" placeholder section per §5.5 with explicit `…` placeholders in every table cell; add the cross-reference one-liner per §5.8; create `scripts/repro/precommit-bootstrap-greenfield.sh` per §5.4. | Goal-spec sections present with verbatim subsection structure, step text, and `…` placeholders in the four hypothesized-mode rows; script is executable and implements the §5.4 responsibilities; one-line cross-reference added to §Evidence. | `specs/goals/20260417-precommit-bootstrap.md`, `scripts/repro/precommit-bootstrap-greenfield.sh`. | — |
| 3 | §5 (execution) | Execute the greenfield reproduction procedure (live runs); commit captured artifacts under `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/`; populate every cell in the four hypothesized-mode rows with `yes`/`no`/`partial`, evidence pointers, and notes; replace the "Newly observed modes" angle-bracketed placeholder with a list or "None observed."; replace the "Confirm-or-Divert Decision" angle-bracketed placeholder with the confirm sentence or a divert paragraph (with follow-up reference). | All `…` placeholders in the four hypothesized-mode rows are replaced with evidence-backed values per §5.9; angle-bracketed placeholders in "Newly observed modes" and "Confirm-or-Divert Decision" are replaced; `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/` exists with the artifact set listed in §5.9. | `specs/goals/20260417-precommit-bootstrap.md` (table cells + decision subsections only — do NOT modify §2-written procedure text), `specs/goals/precommit-bootstrap-repro-artifacts/<YYYY-MM-DD>/` (new directory + contents). | `["2"]` |

---

## 9. Out of Scope (this architecture task)

- `kind` field schema, `OutputEntry`/`Task` struct edits, `proceed.go` dedup logic — owned by `architecture-1` (MERGED).
- `internal/precommit/` package, git-plumbing helpers, `RoleContextData` extension, supervisor-error routing — owned by `architecture-2`.
- ADR-0036 amendment or supersession — owned by `architecture-4`.
- (None — execution of the reproduction run is now explicitly owned by output[3] per §5.9.)
- Hook-version selection per ecosystem — deferred per goal-spec Out of Scope.
- Brownfield migration of partial/broken `.pre-commit-config.yaml` — explicitly out of goal scope.
