# Architecture: CLI RBAC via --agent-id Role Validation

**Task:** architecture-1
**Spec:** specs/goals/20260412-cli-native-access-control.md (Section 1)
**Scope:** Role validation for state-mutating CLI commands using existing identity sources.

## 1. Problem Summary

The CLI currently resolves agent identity (positional arg, `--agent-id` flag, auto-resolved orchestrator) but does not validate whether the resolved role is authorized for the command being invoked. The MCP server performs this validation via `withRole` middleware, but CLI commands bypass it entirely. This architecture adds equivalent RBAC checks to the CLI layer.

## 2. Component Boundaries

### 2.1 RBAC Validation Module (NEW: `cmd/liza/rbac.go`)

**Responsibility:** Provide reusable authorization functions for CLI commands. Each function loads the pipeline resolver, extracts the agent's role, and checks authorization.

**Not responsible for:** Identity resolution (handled by existing `requireAgentID`, `resolveOrchestratorID`), ops-layer logic, error formatting beyond the error message.

**Two authorization modes** (matching MCP middleware patterns):

| Mode | Function | When to use |
|---|---|---|
| Operation-based | `validateAllowedOperation(projectRoot, agentID, opName)` | Command's operation name is in pipeline YAML `allowed-operations` |
| Type-based | `validateRoleType(projectRoot, agentID, allowedTypes...)` | Command uses role-type check (not in `allowed-operations` list) |

**Design rationale:** These mirror MCP's `operationChecker` and `typeChecker` (`internal/mcp/middleware.go:96-127`). The CLI versions are standalone functions (not closures) since CLI commands are one-shot, not registered handlers. They take `projectRoot` to load the resolver on each call.

### 2.2 Command Layer (MODIFIED: `cmd/liza/cmd_task.go`, `cmd/liza/cmd_review.go`, `cmd/liza/cmd_worktree.go`)

**Change:** Each state-mutating command's `RunE` function gains a validation call after identity resolution and before the `commands.*Command()` call.

**Unchanged:** Identity resolution functions (`requireAgentID`, `resolveOrchestratorID`), command registration, flag definitions, argument parsing.

### 2.3 Existing Components (UNCHANGED)

| Component | Role in RBAC |
|---|---|
| `internal/identity/` | `ExtractRole(agentID)` extracts role from agent ID format. Called by validation functions. |
| `internal/pipeline/` | `Resolver.AllowedOperations(role)` returns allowed ops. `Resolver.RoleType(role)` returns role type. Called by validation functions. |
| `internal/commands/` | Command handlers. No RBAC logic added here — validation happens in CLI layer before handler call. |
| `internal/ops/` | Ops layer. Unchanged — authorization is a CLI-layer concern. |

## 3. Interface Contracts

### 3.1 Validation Function Signatures

```go
// validateAllowedOperation checks that the agent's role has the given
// operation in its pipeline YAML allowed-operations list.
// Returns nil if allowed, descriptive error if not.
func validateAllowedOperation(projectRoot, agentID, operationName string) error

// validateRoleType checks that the agent's role type (doer/reviewer/orchestrator)
// matches one of the allowed types.
// Returns nil if allowed, descriptive error if not.
func validateRoleType(projectRoot, agentID string, allowedTypes ...string) error
```

### 3.2 Error Contracts

Validation errors are returned as `error` from `RunE`, which Cobra prints to stderr and exits non-zero. Error messages must be actionable — stating what was attempted, what role was found, and what was expected.

**Operation-based error:**
```
operation "submit-verdict" not allowed for role "coder" (agent coder-1)
```

**Type-based error:**
```
command requires role type [doer] but agent "orchestrator-1" has type "orchestrator"
```

**Resolver load failure (fail-closed):**
```
cannot authorize operation: failed to load pipeline config: <underlying error>
```

The fail-closed behavior matches MCP middleware: if the pipeline config cannot be loaded, all operations are denied.

### 3.3 Operation Name Mapping

CLI command names map directly to pipeline YAML operation names — both use kebab-case. No conversion is needed (unlike MCP's `liza_submit_for_review` -> `submit-for-review` via `mcpToolToOperation`).

| CLI Command | Operation Name (for `validateAllowedOperation`) |
|---|---|
| `submit-for-review` | `submit-for-review` |
| `handoff` | `handoff` |
| `submit-verdict` | `submit-verdict` |
| `mark-blocked` | `mark-blocked` |
| `write-checkpoint` | `write-checkpoint` |
| `set-task-output` | `set-task-output` |
| `await-verdict` | `await-verdict` |
| `await-resubmission` | `await-resubmission` |
| `add-tasks` | `add-tasks` |
| `supersede-task` | `supersede-task` |
| `cancel-task` | `cancel-task` |
| `sprint-checkpoint` | `sprint-checkpoint` |
| `analyze` | `analyze` |
| `delete-agent` | `delete-agent` |
| `clear-stale-review-claims` | `clear-stale-review-claims` |
| `update-sprint-metrics` | `update-sprint-metrics` |
| `set-discovery-disposition` | `set-discovery-disposition` |

## 4. Data Flow

### 4.1 Command Execution Flow (Before vs After)

**Before (no RBAC):**
```
CLI args → identity resolution → commands.*Command() → ops layer → state mutation
```

**After (with RBAC):**
```
CLI args → identity resolution → RBAC validation → commands.*Command() → ops layer → state mutation
                                      |
                                      ├─ Load pipeline config (pipeline.LoadFrozen)
                                      ├─ Create resolver (pipeline.NewResolver)
                                      ├─ Extract role (identity.ExtractRole)
                                      └─ Check allowed-operations or role type
```

### 4.2 Validation Placement Per Identity Pattern

**Pattern 1: Positional `<agent-id>` (claim-task)**

```go
// cmd_task.go — claimTaskCmd.RunE
RunE: func(cmd *cobra.Command, args []string) error {
    taskID := args[0]
    agentID := args[1]

    projectRoot, err := requireProjectRoot()
    if err != nil { return err }

    // NEW: RBAC validation
    if err := validateRoleType(projectRoot, agentID, "doer"); err != nil {
        return err
    }

    return commands.ClaimTaskCommand(projectRoot, taskID, agentID)
}
```

**Pattern 2: `--agent-id` flag (submit-for-review, handoff, submit-verdict, etc.)**

```go
// cmd_review.go — submitForReviewCmd.RunE
RunE: func(cmd *cobra.Command, args []string) error {
    taskID := args[0]
    commitSHA := args[1]

    agentID, err := requireAgentID(cmd)
    if err != nil { return err }

    projectRoot, err := requireProjectRoot()
    if err != nil { return err }

    // NEW: RBAC validation
    if err := validateAllowedOperation(projectRoot, agentID, "submit-for-review"); err != nil {
        return err
    }

    return commands.SubmitForReviewCommand(projectRoot, taskID, commitSHA, agentID)
}
```

**Pattern 3: Auto-resolved orchestrator (add-task, add-tasks, supersede-task, etc.)**

For commands whose operation IS in `allowed-operations` (add-tasks, supersede-task, cancel-task):
```go
// cmd_task.go — addTasksCmd.RunE (after orchestratorID resolution)
if err := validateAllowedOperation(projectRoot, orchestratorID, "add-tasks"); err != nil {
    return err
}
```

For commands whose operation is NOT in `allowed-operations` (add-task, assess-blocked, assess-hypothesis-exhausted):
```go
// cmd_task.go — addTaskCmd.RunE (after orchestratorID resolution)
if err := validateRoleType(projectRoot, orchestratorID, "orchestrator"); err != nil {
    return err
}
```

**Pattern 4: wt-merge normalization**

`wt-merge` (`cmd_worktree.go:91-123`) currently has inline RBAC that loads the pipeline config and checks role type. This will be refactored to use `validateRoleType(projectRoot, agentID, "reviewer")`, removing the inline resolver loading.

## 5. RBAC Coverage Matrix

Complete mapping of all CLI commands to their RBAC treatment:

| Command | Identity Source | RBAC Check | Check Function |
|---|---|---|---|
| **State-mutating with RBAC** | | | |
| `claim-task` | Positional `<agent-id>` | Type: doer | `validateRoleType` |
| `submit-for-review` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `handoff` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `submit-verdict` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `mark-blocked` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `write-checkpoint` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `set-task-output` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `await-verdict` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `await-resubmission` | `--agent-id` flag | Operation | `validateAllowedOperation` |
| `wt-merge` | `--agent-id` flag | Type: reviewer | `validateRoleType` |
| `add-task` | Auto-resolved orch | Type: orchestrator | `validateRoleType` |
| `add-tasks` | Auto-resolved orch | Operation | `validateAllowedOperation` |
| `supersede-task` | Auto-resolved orch | Operation | `validateAllowedOperation` |
| `cancel-task` | Auto-resolved orch | Operation | `validateAllowedOperation` |
| `assess-blocked` | Auto-resolved orch | Type: orchestrator | `validateRoleType` |
| `assess-hypothesis-exhausted` | Auto-resolved orch | Type: orchestrator | `validateRoleType` |
| **Excluded from RBAC** | | | |
| `pause`, `stop`, `start`, `resume`, `replan`, `release-claim` | `--changed-by` | No RBAC | Human/supervisor operations |
| `get`, `status`, `validate`, `version` | None | No RBAC | Read-only |
| `tui`, `init`, `delete task`, `proceed` | None | No RBAC | Interactive/human-only |
| `wt-create`, `wt-delete` | None | No RBAC | No identity source (v2 consideration) |
| `analyze`, `sprint-checkpoint`, `update-sprint-metrics`, `clear-stale-review-claims` | None | No RBAC | No identity source in CLI |
| `set-discovery-disposition` | None | No RBAC | No identity source in CLI |

**Note on CLI vs MCP coverage divergence:** The MCP server validates `analyze`, `sprint-checkpoint`, `update-sprint-metrics`, `clear-stale-review-claims`, `wt-create`, `wt-delete`, and `set-discovery-disposition` because MCP handlers always receive `agent_id` as a parameter. The CLI versions of these commands take no agent identity, so RBAC cannot be applied. Per the spec: "Commands excluded from RBAC ... Read-only or human-only — no RBAC needed."

## 6. Cross-Cutting Concerns

### 6.1 Pipeline Config Loading

Both `validateAllowedOperation` and `validateRoleType` need a `pipeline.Resolver`. They call `pipeline.LoadFrozen(projectRoot)` internally. This is the same loading path used by:
- `resolveOrchestratorID` (main.go:79)
- `wt-merge` inline RBAC (cmd_worktree.go:109)
- MCP server initialization (internal/mcp/server.go:41)

Loading is idempotent (reads and parses `.liza/pipeline.yaml`). For commands that already load the resolver (e.g., `resolveOrchestratorID`), the double-load is acceptable: the file is small, parsing is fast, and the alternative (threading the resolver through the call chain) adds complexity for negligible performance gain.

### 6.2 Fail-Closed Behavior

If `pipeline.LoadFrozen` fails, the validation functions return an error that prevents command execution. This matches MCP's behavior (`middleware.go:72-77`): a nil resolver blocks all operations.

### 6.3 Agent ID Format Validation

`identity.ExtractRole` validates agent ID format as a side effect. Invalid formats (e.g., "coder" without a number suffix) produce clear errors. This is the same validation MCP performs via `identity.ValidateFormat` before role extraction.

### 6.4 No Syntax Changes

Per the spec: "No syntax changes. `claim-task` keeps its positional `<agent-id>`. The validation is internal." All commands retain their current argument/flag syntax. RBAC validation is invisible to callers when authorization succeeds.

## 7. Test Strategy

### 7.1 Unit Tests for Validation Functions (`cmd/liza/rbac_test.go`)

| Test | What it validates |
|---|---|
| `TestValidateAllowedOperation_CoderCanSubmitForReview` | Happy path: coder role has `submit-for-review` in allowed-operations |
| `TestValidateAllowedOperation_CoderCannotSubmitVerdict` | Rejection: coder role does not have `submit-verdict` |
| `TestValidateAllowedOperation_ReviewerCanSubmitVerdict` | Happy path: reviewer role has `submit-verdict` |
| `TestValidateAllowedOperation_UnknownRole` | Error on invalid role name |
| `TestValidateAllowedOperation_InvalidAgentIDFormat` | Error on malformed agent ID |
| `TestValidateRoleType_DoerAllowed` | Happy path: coder has type "doer" |
| `TestValidateRoleType_OrchestratorBlocked` | Rejection: orchestrator is not type "doer" |
| `TestValidateRoleType_InvalidAgentID` | Error on malformed agent ID |

### 7.2 Integration Tests for Command Wiring (`cmd/liza/mutation_wiring_test.go`)

Extend the existing `TestMutationCommandWiring` with RBAC violation cases:

| Test | Identity Pattern | What it validates |
|---|---|---|
| `claim-task rejects orchestrator agent` | Positional | `orchestrator-1` cannot claim (not a doer) |
| `claim-task accepts coder agent` | Positional | `coder-1` can claim (is a doer) |
| `submit-verdict rejects coder agent` | `--agent-id` | `coder-1` cannot submit verdict |
| `submit-for-review rejects reviewer agent` | `--agent-id` | `code-reviewer-1` cannot submit for review |
| `add-tasks rejects non-orchestrator` | Auto-resolved | Non-orchestrator `--agent-id` override rejected |
| `wt-merge rejects coder agent` | `--agent-id` | `coder-1` cannot merge (not a reviewer) |
| `RBAC error message is actionable` | Any | Error includes operation, role, and agent ID |

### 7.3 Testing Approach

Tests use the existing `executeRootCommand` helper (`mutation_wiring_test.go:228`) which runs commands against a temp project root with full pipeline config. The `testhelpers.SetupPipelineConfig` function provides a real pipeline YAML with role definitions, ensuring RBAC checks use production-equivalent configuration.

For RBAC rejection tests: call the command with a role that doesn't have the required permission, assert the error contains the expected rejection message, and verify no state mutation occurred.

## 8. Decomposition into Code-Planning Scopes

### Scope 0: RBAC Validation Module

Create the two authorization functions and their unit tests.

**Files:**
- `cmd/liza/rbac.go` (new) — `validateAllowedOperation`, `validateRoleType`
- `cmd/liza/rbac_test.go` (new) — Unit tests for both functions

**Done when:** `validateAllowedOperation` and `validateRoleType` exist in `cmd/liza/rbac.go`, handle resolver load failures with fail-closed behavior, return actionable error messages including operation/role/agent-id, and pass unit tests covering happy path, rejection, invalid format, and unknown role cases.

### Scope 1: Wire RBAC Into State-Mutating Commands

Add validation calls to all state-mutating commands that have identity sources (per Section 5 RBAC Coverage Matrix). Normalize wt-merge inline RBAC to use new helpers.

**Files:**
- `cmd/liza/cmd_task.go` (modify) — claim-task, add-task, add-tasks, supersede-task, cancel-task, mark-blocked, assess-blocked, assess-hypothesis-exhausted, write-checkpoint, set-task-output
- `cmd/liza/cmd_review.go` (modify) — submit-for-review, handoff, submit-verdict, await-verdict, await-resubmission
- `cmd/liza/cmd_worktree.go` (modify) — wt-merge normalization
- `cmd/liza/mutation_wiring_test.go` (modify) — RBAC rejection integration tests

**Done when:** All 16 commands listed in the RBAC Coverage Matrix with "State-mutating with RBAC" have validation calls. wt-merge inline RBAC (cmd_worktree.go:99-119) is replaced by `validateRoleType`. Integration tests verify RBAC rejection for each identity pattern (positional, --agent-id flag, auto-resolved orchestrator). `liza claim-task orchestrator-1 T1` returns a role-type error. `liza submit-verdict T1 APPROVED --agent-id coder-1` returns an allowed-operation error.

**Depends on:** Scope 0 (needs the validation functions).
